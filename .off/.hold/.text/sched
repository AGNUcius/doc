Related: at, access, allocate, constrain, cron, share, task, time, use

$ schtasks.exe >>Enables an administrator to create, delete, query, change, run and end scheduled tasks on a local or remote system. Replaces AT.exe.

