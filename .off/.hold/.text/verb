Related: antonym, command, lang, noun, speak, synonym

setup, add, install

start, boot, init, go, play, run
stop, break, halt, pause

configure, setup
build, make, compile

download
kill/stop

list, ls, dir

read, get, rip
write, set, put, post, send, burn

mount, connect
del, uninstall, release

compress, minimize, zip, archive
extract, open, decompress, deflate, unzip

status, errors, logs

reset, renew, reconfigure, rerun, restart

check, verify


lock, unlock

exist, exists

rm, del, delete, remove, purge, uninstall

edit, search, replace

simulate

ASSOCIATIONS:
iso, zip

